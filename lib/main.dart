/*import 'package:flutter/material.dart';
import 'package:shout_out/profile_page.dart'; // Importing the ProfilePage widget

void main() {
  runApp(const MyApp()); // Running the ProfilePage widget as the main app
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});
  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: ProfilePage(), // Setting ProfilePage as the home screen
    );
  }
}*/
